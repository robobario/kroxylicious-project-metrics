{
  "number": 4808,
  "title": "Per-route filter's out-of-band sendRequest() response is delivered to the client, not the filter, when 2+ routes send OOB requests on one connection",
  "state": "open",
  "labels": []
}
