# CLAUDE.md

## Git remotes

`origin` points directly to the personal fork (`robobario/kroxylicious-project-metrics`).
Push feature branches to `origin` and open PRs against `origin/main`.
There is no separate `rob` remote for this project.

## Running tests

```bash
python3 -m pytest
```

## Previewing the site locally

```bash
./dev-preview.sh
```
